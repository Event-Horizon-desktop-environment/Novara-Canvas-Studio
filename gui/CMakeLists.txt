find_package(Qt6 REQUIRED COMPONENTS Widgets OpenGLWidgets Svg Test)

include(GNUInstallDirs)
get_filename_component(CANVAS_SOURCE_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/.." ABSOLUTE)

find_package(PkgConfig REQUIRED)
pkg_check_modules(PIPEWIRE IMPORTED_TARGET libpipewire-0.3)
pkg_check_modules(ALSA IMPORTED_TARGET alsa)

pkg_check_modules(EGL IMPORTED_TARGET egl)

set(CMAKE_AUTOMOC ON)
set(CMAKE_AUTORCC ON)
set(CMAKE_AUTOUIC ON)
set(CMAKE_AUTOUIC_SEARCH_PATHS ui)

qt_standard_project_setup()

qt_add_executable(canvas
    src/main.cpp

    resources.qrc
    src/UX/MainWindow.cpp
    src/UX/MainWindowShell.cpp
    src/UX/ShellMenus.cpp
    src/UX/ShellTopBar.cpp
    src/UX/ShellPageBar.cpp
    src/UX/ShellTransportBar.cpp
    src/UX/ShellMediaDock.cpp
    src/UX/ShellInspectorDock.cpp
    src/UX/ShellDeliverPage.cpp
    src/UX/ShellCenter.cpp
    src/UX/InspectorVisual.cpp
    src/UX/InspectorAudio.cpp
    src/UX/InspectorAudioEq.cpp
    src/UX/InspectorTransition.cpp
    src/UX/InspectorFile.cpp
    src/UX/InspectorSubtitles.cpp
    src/UX/InspectorShared.hpp
    src/UX/ActionSearch.cpp
    src/UX/ActionSearch.hpp
    src/UX/KeyboardCustomizationDialog.cpp
    src/UX/KeyboardCustomizationDialog.hpp
    src/UX/InspectorVisual.hpp
    src/UX/InspectorAudio.hpp
    src/UX/InspectorTransition.hpp
    src/UX/InspectorFile.hpp
    src/UX/InspectorSubtitles.hpp
    src/Widgets/timeline_snap.cpp
    src/Widgets/timeline_selection.cpp
    src/Widgets/timeline_drag.cpp
    src/Widgets/transition_handle_editor.cpp
    src/Widgets/timeline_volume_line.cpp
    src/UX/theme_clip_colors.cpp
    src/UX/theme_icons.cpp
    src/UX/theme_menu.cpp
    src/UX/theme_state.cpp
    src/UX/theme_styles.cpp
    src/UX/theme_tokens.cpp
    src/UX/horizon_style.cpp
    src/UX/SettingsDialog.cpp
    ui/MainWindow.ui

    src/features/app/AppActions.cpp
    src/features/project/ProjectActions.cpp
    src/features/project/new_project_dialog.cpp
    src/features/project/new_project_dialog.hpp
    src/features/project/project_manager_widget.cpp
    src/features/project/project_manager_widget.hpp
    src/features/timeline/TimelineActions.cpp
    src/features/timeline/audio_targets.cpp
    src/features/timeline/SubtitleActions.cpp
    src/features/timeline/subtitle_dialog.cpp
    src/features/timeline/ThreePointActions.cpp
    src/features/timeline/timeline_view_options.cpp
    src/features/timeline/view_options_menu.cpp
    src/features/shortcuts/shortcut_catalog.cpp
    src/features/shortcuts/shortcut_manager.cpp
    src/features/shortcuts/ShortcutActions.cpp

    src/features/playback/sequence_controller.cpp
    src/features/playback/audio_pipeline.cpp
    src/features/playback/timeline_decoder.cpp
    src/features/playback/vaapi_import_state.cpp
    src/features/playback/audio_output.cpp
    src/features/playback/sonicsync.cpp
    src/features/playback/playback_controller.cpp
    src/features/thumbnails/thumbnail_service.cpp

    src/features/deliver/deliver_settings_panel.cpp
    src/features/deliver/deliver_settings_model.cpp
    src/features/deliver/render_queue_panel.cpp
    src/features/deliver/DeliverActions.cpp

    src/features/source_preview/source_preview_model.cpp
    src/features/source_preview/source_preview_controller.cpp
    src/features/source_preview/source_viewer_panel.cpp

    src/features/color/color_widgets.cpp
    src/features/color/curves/curves_panel.cpp
    src/features/color/mini_timeline_strip.cpp
    src/features/color/node_graph_canvas.cpp
    src/features/color/scopes/common/scope_common.cpp
    src/features/color/scopes/parade/parade_scope.cpp
    src/features/color/scopes/waveform/waveform_scope.cpp
    src/features/color/scopes/vectorscope/vectorscope_scope.cpp
    src/features/color/scopes/histogram/histogram_scope.cpp
    src/features/color/scopes/chromaticity/chromaticity_widget.cpp
    src/features/color/color_page.cpp

    src/Widgets/viewer_gl.cpp
    src/Widgets/keyboard_map_widget.cpp
    src/Widgets/media_pool_widget.cpp
    src/Widgets/timeline_widget.cpp
    src/Widgets/timeline_view.cpp
    src/Widgets/timeline_interaction.cpp
    src/Widgets/timeline_thumbnails.cpp
    src/Widgets/toolbox_widget.cpp
    src/Widgets/viewport_selector.cpp
    src/Widgets/vaapi_viewer.cpp
)

target_include_directories(canvas PRIVATE src ui)

target_link_libraries(canvas PRIVATE
    Qt6::Widgets
    Qt6::OpenGLWidgets
    Qt6::Svg
    canvas_core
)

if(PIPEWIRE_FOUND)
    target_link_libraries(canvas PRIVATE PkgConfig::PIPEWIRE)
    target_compile_definitions(canvas PRIVATE CANVAS_HAVE_PIPEWIRE)
endif()
if(ALSA_FOUND)
    target_link_libraries(canvas PRIVATE PkgConfig::ALSA)
    target_compile_definitions(canvas PRIVATE CANVAS_HAVE_ALSA)
endif()
if(EGL_FOUND)
    target_link_libraries(canvas PRIVATE PkgConfig::EGL)
    target_compile_definitions(canvas PRIVATE CANVAS_HAVE_EGL)
endif()

install(TARGETS canvas
    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
)

install(FILES "${CMAKE_CURRENT_SOURCE_DIR}/resources/icons/app_icon.svg"
    DESTINATION ${CMAKE_INSTALL_DATADIR}/icons/hicolor/scalable/apps
    RENAME canvas.svg
)

install(FILES "${CANVAS_SOURCE_ROOT}/packaging/canvas.desktop"
    DESTINATION ${CMAKE_INSTALL_DATADIR}/applications
)

add_subdirectory(tests)
